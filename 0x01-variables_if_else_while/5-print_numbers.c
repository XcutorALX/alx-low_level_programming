#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0
 */

int main(void)
{
	int myDigit;

	for (myDigit = 0; myDigit < 10; myDigit++)
	{
		printf("%d", myDigit);
	}
	printf("\n");
	return (0);
}

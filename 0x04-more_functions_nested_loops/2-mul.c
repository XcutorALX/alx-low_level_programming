#include "main.h"
/**
 * mul - multiplies two integers
 * @a: first number
 * @b: second number
 *
 * Return: returns the multiple
 */
int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}

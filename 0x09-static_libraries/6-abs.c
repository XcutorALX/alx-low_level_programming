#include "main.h"
/**
 * _abs - checks for uppe and lower case
 *@c: the character to be checked
 *
 * Return: returns the absolute of values
 */
int _abs(int c)
{
	if (c > 0)
		return (c);
	else
		return (c * -1);
}
